# Systematic LTL scaling study

The manifest contains 94 instances from eight language families. Window, alphabet, and nesting parameters use powers of two. Each successful route is measured three times; terminal failures are measured once.
